# 前端环境变量示例
# 本地开发时复制此文件为 .env.local 并修改
# 部署到Vercel时在项目设置中添加此环境变量

# 本地开发（默认值，不设也行）
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:5000

# 部署后替换为你的Render后端地址，例如：
# NEXT_PUBLIC_API_BASE_URL=https://community-goods-api.onrender.com
